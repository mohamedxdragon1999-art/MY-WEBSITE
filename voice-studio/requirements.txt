# FULL install - unlocks EVERY voice mode plus optional server-side STT.
#
# You do NOT need this for the one-click launcher: it installs a tiny core
# (requirements-core.txt) and the site works immediately with the online
# "Current voice" + the browser voice. Install this only if you also want the
# fully-OFFLINE local voices (Kokoro / Piper) or server-side speech-to-text.
#
# Version FLOORS (>=) - not exact pins - so pip can pick a prebuilt wheel for
# your Python version instead of trying to compile from source.

# --- core web server (required) ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5,<3
python-multipart>=0.0.9
numpy>=1.24

# --- Mode 1: Current voice (edge-tts) --- online Microsoft neural voices, no key
edge-tts>=6.1

# --- Mode 4: NVIDIA Magpie (cloud NIM) + free cloud AI brain --- needs internet
httpx>=0.26

# --- Mode 2: Pocket TTS (Piper) --- fully offline local-CPU neural TTS
piper-tts>=1.2.0

# --- Mode 3: Kokoro TTS --- open-weight 82M voice, CPU via onnxruntime
kokoro-onnx>=0.4.4
soundfile>=0.12
onnxruntime>=1.17

# --- Optional: server-side speech-to-text (used only if a browser lacks the
#     built-in Web Speech API). Uncomment to enable the /api/stt fallback. ---
# faster-whisper>=1.0

# --- v5 Ultra Human mode ---------------------------------------------------
# Chatterbox = local MIT voice preferred over ElevenLabs in 63.75% of blind
# tests. Optional: the Ultra Human mode also works with just FISH_API_KEY
# (free cloud) and falls back to Kokoro/Edge if neither is present.
# Uncomment to install locally (large download, first run fetches weights):
# chatterbox-tts>=0.1.4
